// Application Configuration
// This file contains global UI configuration settings for the entire application

export default defineAppConfig({
  // UI Theming Configuration
  // Controls the color schemes and appearance across the site
  ui: {
    // Color Palette Definition
    // Sets primary and neutral colors used throughout the UI components
    colors: {
      primary: 'green', // Primary brand color for buttons, links, and accents
      neutral: 'slate', // Neutral color for backgrounds, borders, and text
    },
  },
})
